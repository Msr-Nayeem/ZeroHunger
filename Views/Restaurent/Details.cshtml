@model ZH.Dbf.Request

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_RestaurentLayout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Request</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.preserveTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.preserveTime)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.status)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.status)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Restaurent.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Restaurent.name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
